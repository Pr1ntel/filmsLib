package org.example.filmslib.repositorys;

import org.example.filmslib.models.StyleFilms;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StyleFilmsRepository extends JpaRepository<StyleFilms, Integer> {
}
